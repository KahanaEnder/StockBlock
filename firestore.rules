rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Acceso solo a usuarios autenticados para todas las colecciones
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}